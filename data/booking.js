document.getElementById("btn").addEventListener(
    "Book Now",
    function(event) {
      if (event.target.value === "Submit") {
        event.target.value = "Submit";
      } else {
        event.target.value = "Submit";
      }
    },
    false
  );
  


